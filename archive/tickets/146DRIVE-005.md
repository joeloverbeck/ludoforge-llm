# 146DRIVE-005: Investigate post-migration preview perf shortfall

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes — repo-owned measurement helper only; no runtime optimization landed
**Deps**: `archive/tickets/146DRIVE-004.md`, `archive/specs/146-scoped-draft-state-for-preview-drive.md`, `specs/147-aot-consideration-ast-compilation.md`

## Problem

146DRIVE-004 successfully migrated the `driveSyntheticCompletion` greedy `chooseOne` branch to `applyPreviewDriveGreedyChooseOne`, but the measured FITL preview-pipeline corpus still misses Spec 146's hard target.

Post-migration direct measurement on 2026-04-26:

- `run1_totalMs=28461.51`
- `run2_totalMs=27425.41`
- `run3_totalMs=27888.67`
- `mean_totalMs=27925.19`
- `mad_pct=1.28`
- `hardTargetMs=25600`
- `pass=false`

The result is meaningfully faster than the post-campaign ~31149 ms baseline but remains about 2325 ms above the hard target. Spec 147 already proposes AOT consideration AST compilation as a likely next optimization, but this ticket must first classify the remaining gap with fresh profile evidence rather than assuming the remaining cost belongs entirely to Spec 147.

## Assumption Reassessment (2026-04-26)

1. `packages/engine/src/agents/policy-preview.ts` now calls `applyPreviewDriveGreedyChooseOne` on the gated greedy `chooseOne` path; the migration itself is landed by 146DRIVE-004.
2. `packages/engine/test/perf/agents/preview-pipeline.perf.test.ts` still emits only the older non-blocking baseline warning. It does not assert the Spec 146 hard target or print `totalMs` when the warning threshold is not exceeded.
3. The hard-target verdict therefore needs either a dedicated harness/report path or a test update before future closeout can rely on the named perf lane alone.
4. The broad determinism lane timed out in `dist/test/determinism/zobrist-incremental-parity.test.js` after 20 minutes during 146DRIVE-004 verification, after both Spec 146 determinism witnesses had passed. That timeout must be classified as pre-existing lane cost, post-migration slowdown, or separate runner threshold drift before closing the remaining performance series.

## Architecture Check

1. The next step is measurement-first, not target tuning. Lowering the 25.6s target is explicitly out of scope for Spec 146 and would be metric gaming.
2. Any production optimization must remain engine-agnostic and use generic policy/kernel surfaces; no FITL-specific fast path is allowed.
3. If Spec 147 is confirmed as the correct owner, this ticket should create or update the first Spec 147 implementation ticket rather than duplicating that design under 146DRIVE.

## What to Change

### 1. Produce fresh profile evidence

Run the preview-pipeline corpus with V8 sampling/profile output and capture the top remaining self-time and bottom-up costs after the 146DRIVE-004 migration.

Classify at minimum:

- residual policy-preview synthetic-completion drive cost
- policy consideration / `resolveRef` / evaluator cost
- Zobrist hashing cost
- test harness / corpus overhead

### 2. Make the hard-target perf verdict durable

Add the narrowest repeatable measurement path that reports:

- three `previewOn_totalMs_ms` samples
- mean
- `mad_pct`
- hard-target comparison against 25600 ms
- candidate budget and sampled action-selection count

Prefer updating the existing perf test or adding a small repo-owned helper if that is cleaner. Do not rely on ad hoc `node -e` snippets as the final evidence path.

### 3. Reconcile the next owner

Based on the profile:

- if Spec 147 owns the remaining gap, create the first active Spec 147 ticket and update Spec 147 with the post-146 baseline
- if a smaller 146-scoped regression or missed migration remains, update this ticket with the concrete fix scope
- if the determinism timeout is a separate lane-budget issue, create or update a separate ticket for that runner/test budget

## Files to Touch

- `packages/engine/test/perf/agents/preview-pipeline.perf.test.ts` (modify, if making the hard target executable there)
- `packages/engine/scripts/measure-preview-pipeline-hard-target.mjs` (new, durable hard-target measurement path)
- `packages/engine/package.json` (modify, package script for the measurement path)
- `packages/engine/src/agents/*` (modify, only if profile proves an agent-runtime fix is owned here)
- `specs/147-aot-consideration-ast-compilation.md` (modify, if confirmed as next owner)
- `tickets/147*.md` or equivalent (new, if Spec 147 ownership is confirmed)

## Out of Scope

- Lowering or weakening the 25.6s target.
- Adding FITL-specific engine or agent branches.
- Reworking `applyPreviewDriveGreedyChooseOne` without profile evidence that the new drive function is still the dominant cost.

## Acceptance Criteria

### Tests That Must Pass

1. A repo-owned measurement command reports the three-run hard-target verdict and either passes `mean_totalMs <= 25600` with `mad_pct < 1.5` or records the exact failure and next owner.
2. If production code changes, `pnpm -F @ludoforge/engine build`, `pnpm -F @ludoforge/engine typecheck`, and the relevant focused tests pass.
3. If a Spec 147 ticket is created, `pnpm run check:ticket-deps` passes.

### Invariants

1. Performance evidence remains tied to the same FITL preview-pipeline corpus: seed 1000, maxTurns 200, playerCount 4, ARVN evolved seat, sampleSize 50, candidateBudget recorded.
2. The hard target remains `25600 ms`; any miss is reported as a miss, not normalized away.
3. Follow-up ownership is explicit in tickets/specs before this ticket closes.

## Test Plan

### New/Modified Tests

1. `packages/engine/scripts/measure-preview-pipeline-hard-target.mjs` — repo-owned measurement command for the Spec 146 hard-target verdict.

### Commands

1. `pnpm -F @ludoforge/engine build`
2. `pnpm -F @ludoforge/engine measure:preview-hard-target`
3. `pnpm -F @ludoforge/engine typecheck`
4. `pnpm run check:ticket-deps`

## Outcome (2026-04-26)

**Completion date**: 2026-04-26

Added a durable hard-target measurement command:

- `packages/engine/scripts/measure-preview-pipeline-hard-target.mjs`
- `pnpm -F @ludoforge/engine measure:preview-hard-target`

The helper mirrors the checked-in `preview-pipeline.perf.test.ts` corpus rather than the campaign runner's agent-guided override: production FITL fixture, seed `1000`, `maxTurns=200`, `playerCount=4`, ARVN evolved seat, `sampleSize=50`, and the live seat profiles `us-baseline`, `arvn-evolved`, `nva-baseline`, `vc-baseline`. It reports three `previewOn_totalMs_ms` samples, mean, mean absolute deviation percent, the `25600 ms` hard target, pass/fail, candidate budget, sampled action-selection count, and a JSON summary.

Fresh V8 CPU profile evidence from `node --cpu-prof --cpu-prof-dir=/tmp/146drive-profile-greedy packages/engine/scripts/measure-preview-pipeline-hard-target.mjs --runs 1`:

- Profiled sample: `previewOn_totalMs_ms=28019.62`, `candidateBudget=465`, `sampledActionSelectionCount=50`, `pass=false`.
- Top aggregated self-time samples: `fnv1a64` 17.68%, garbage collector 11.28%, kernel `resolveRef` 6.09%, `buildTokenStateIndex` 4.71%, `evalCondition` 4.09%, `evalValue` 2.89%, spatial `evaluateVia` 2.78%, `canonicalizeHashValue` 2.74%, `digestDecisionStackFrame` 1.08%.
- Bottom-up sample attribution keeps the preview drive on the path (`driveSyntheticCompletion` 48.52%, `publishMicroturnFromCanonicalState` 49.63%), but the remaining work is dominated by policy evaluation / preview-surface resolution and generic kernel viability/query/hash machinery rather than a small missed 146 migration. `policy-evaluation-core:evaluateExpr` and `resolveRef` dominate the policy side, matching Spec 147's AOT consideration-evaluator ownership.

Classification:

- residual policy-preview synthetic-completion drive cost: still material, but not isolated to a missed `applyPreviewDriveGreedyChooseOne` caller migration.
- policy consideration / `resolveRef` / evaluator cost: confirmed as the next large owner; assigned to Spec 147.
- Zobrist hashing cost: still visible, but the profile no longer supports another narrow Spec 146 shadow-chain fix as the next step.
- test harness / corpus overhead: compilation is outside the measured `totalMs` in the durable helper; the sample count and candidate budget remain stable.

Created follow-up owner `tickets/147AOTCON-001.md` and updated `specs/147-aot-consideration-ast-compilation.md` with the post-146 baseline and profile classification. No production runtime optimization was made under this ticket because the evidence points to the broader Spec 147 shared-contract implementation.

Final verification:

1. `pnpm -F @ludoforge/engine build` — passed.
2. `pnpm -F @ludoforge/engine measure:preview-hard-target` — command passed and reported the hard-target miss: `previewOn_totalMs_ms=27843.85,26752.56,26707.75`, `mean_totalMs=27101.39`, `mad_pct=1.83`, `hardTargetMs=25600`, `pass=false`, `candidateBudget=465`, `sampledActionSelectionCount=50`.
3. `pnpm -F @ludoforge/engine typecheck` — passed.
4. `pnpm run check:ticket-deps` — passed before archival with active follow-up `tickets/147AOTCON-001.md`.

Outcome amended: 2026-04-26 — updated archived dependency reference after Spec 146 moved to `archive/specs/146-scoped-draft-state-for-preview-drive.md`.
