# 82EFFASTTYPTAG-005: Schema Serialization Update for _k Field

**Status**: ✅ COMPLETED
**Priority**: HIGH
**Effort**: Small
**Engine Changes**: Yes — `packages/engine/src/kernel/schemas-ast.ts`, schema artifacts
**Deps**: 82EFFASTTYPTAG-001 (needs `EFFECT_KIND_TAG`)

## Problem

The Zod schemas in `schemas-ast.ts` and the JSON Schema artifacts in
`packages/engine/schemas/` do not include the `_k` field. Per Foundation 5
(round-trip integrity) and Foundation 9 (no backwards-compat shims), `_k`
must be serialized in GameDef JSON as a required integer field. The schema
must validate its presence.

## Assumption Reassessment (2026-03-25)

1. `schemas-ast.ts` defines `effectAstSchemaInternal` as a Zod union of 34
   effect schemas (lines 539-941). Each schema wraps the payload in its kind
   key. Confirmed.
2. The schema is lazy-initialized via `EffectASTSchema` (line 123) to support
   recursion. Confirmed.
3. JSON Schema artifacts are generated by `pnpm turbo schema:artifacts` and
   live in `packages/engine/schemas/`. Confirmed.
4. `assertValidatedGameDef` uses these schemas for validation. Confirmed.

## Architecture Check

1. Adding `_k` as a required `z.number().int()` field to each of the 34
   effect schema entries is the correct approach — it mirrors the type-level
   change from ticket 001.
2. Per Foundation 9, `_k` is required — no optional fallback.
3. Schema artifacts must be regenerated after the Zod schema update.

## What to Change

### 1. Update `schemas-ast.ts` — ALREADY DONE

All 34 effect schema entries in `effectAstSchemaInternal` already include
`_k: IntegerSchema` as a required field (added as part of tickets 001-004).

### 2. Regenerate JSON Schema artifacts — ALREADY DONE

JSON Schema artifacts in `packages/engine/schemas/` already include `_k` as
a required integer field on all 34 effect variants. Verified via deep scan
of `GameDef.schema.json`.

### 3. Add explicit rejection test for missing `_k` — REMAINING

Add a test to `schemas-ast.test.ts` that constructs an EffectAST literal
without `_k` and asserts that `EffectASTSchema.safeParse()` rejects it.
This covers AC #3.

## Files to Touch

- `packages/engine/src/kernel/schemas-ast.ts` (modify)
- `packages/engine/schemas/*.json` (regenerated)

## Out of Scope

- Type definitions in `types-ast.ts` — ticket 001
- `makeEffect()` and `tagEffectAsts()` — ticket 002
- Dispatch/registry changes — ticket 003
- Compiler migration — ticket 004
- Test fixture migration — ticket 006
- CI tests — ticket 007
- Changes to `ValueExpr` `_t` schema handling
- Changes to `ConditionAST` schemas
- Worker transfer / Comlink changes (structuredClone preserves `_k`
  automatically)

## Acceptance Criteria

### Tests That Must Pass

1. `pnpm turbo schema:artifacts` succeeds and produces updated JSON Schema
   files that include `_k` as a required integer.
2. `assertValidatedGameDef` accepts GameDef JSON with `_k` fields on all
   effect nodes.
3. `assertValidatedGameDef` rejects GameDef JSON missing `_k` on any effect
   node.
4. Existing suite: `pnpm -F @ludoforge/engine test`

### Invariants

1. `_k` is a required integer field in the schema — no optional fallback.
2. Schema artifacts are in sync with Zod schema definitions.
3. No changes to ConditionAST or ValueExpr schemas.
4. Round-trip: serialize GameDef → deserialize → validate passes with `_k`.

## Test Plan

### New/Modified Tests

1. Extend existing schema validation tests (if any) to verify `_k` presence.
2. Add a test that constructs a GameDef with effects missing `_k` and
   asserts validation failure.

### Commands

1. `pnpm turbo schema:artifacts`
2. `pnpm -F @ludoforge/engine build && pnpm -F @ludoforge/engine test`
3. `pnpm turbo typecheck`

## Outcome

- **Completion date**: 2026-03-25
- **What changed**:
  - Zod schemas (`schemas-ast.ts`) and JSON Schema artifacts already included `_k` as a required integer field from tickets 001-004. No schema changes needed.
  - Added two tests to `packages/engine/test/unit/schemas-ast.test.ts`:
    1. Rejection test: verifies `EffectASTSchema` rejects 5 representative effect kinds (`setVar`, `addVar`, `moveToken`, `if`, `forEach`) when `_k` is missing.
    2. Acceptance test: verifies `EffectASTSchema` accepts `setVar` with valid `_k`.
- **Deviations**: Schema update work was already done by earlier tickets; only the explicit rejection test was missing.
- **Verification**: 4775/4775 engine tests pass, typecheck clean, `schema:artifacts` succeeds.
