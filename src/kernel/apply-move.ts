import type { ApplyMoveResult, GameDef, GameState, Move } from './types.js';

export const applyMove = (_def: GameDef, _state: GameState, _move: Move): ApplyMoveResult => {
  throw new Error('applyMove is not implemented in KERGAMLOOTRI-001');
};
