import type { GameDef, GameState, Move } from './types.js';

export const legalMoves = (_def: GameDef, _state: GameState): readonly Move[] => {
  throw new Error('legalMoves is not implemented in KERGAMLOOTRI-001');
};
