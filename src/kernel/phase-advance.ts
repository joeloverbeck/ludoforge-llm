import type { GameDef, GameState } from './types.js';

export const advancePhase = (_def: GameDef, _state: GameState): GameState => {
  throw new Error('advancePhase is not implemented in KERGAMLOOTRI-001');
};
