# 151ENVISO-002: Write `docs/engine-environment-isolation.md` convention doc

**Status**: PENDING
**Priority**: MEDIUM
**Effort**: Small
**Engine Changes**: None — documentation only
**Deps**: `archive/tickets/151ENVISO-001.md`

## Problem

Spec 151 D1 calls for a one-page reference doc explaining the browser-safe vs. Node-only file convention, the `*-node-loader.ts` naming pattern, and which engine subpath exports are browser-safe. Without this doc, future contributors hit the ESLint rule's error message (introduced in 003) without context for why the rule exists or how to fix violations. The doc also serves as the canonical place for the audit-finalized ignore list — per spec AC #3: "the ignores list is documented in `docs/engine-environment-isolation.md`".

## Assumption Reassessment (2026-05-05)

1. `docs/engine-environment-isolation.md` does not yet exist — verified during spec 151 reassessment.
2. After 151ENVISO-001 lands, the engine source has its renames executed and the per-file classification is final, so the doc can list final paths without pending decisions.
3. Engine subpath exports verified during spec 151 reassessment (in `packages/engine/package.json:6-31`): `./` (kernel/index.ts), `./runtime` (kernel/runtime.ts), `./cnl`, `./agents`, `./sim`, `./trace`. Runner usage measured against `packages/runner/src`: `./runtime` 66 imports, `./trace` 3, `./agents` 1, `./` 0. The `./` (root) classification as browser-safe is forward-compatible policy, not current usage.

## Architecture Check

1. A documented convention is the durable enforcement complement to the ESLint rule: the rule (003) blocks violations mechanically; the doc explains the underlying reasoning so the next contributor can extend the convention to a new file or subpath without re-deriving it.
2. Foundation 5 (One Rules Protocol, Many Clients): the doc names which subpaths the engine ships safely to browser hosts and which require Node, so consumers can pick the right import path without trial-and-error CI cycles.
3. Single source of truth for the classification — referenced by the ESLint rule's error message (003), by spec 151, and by future contributors. No duplicate convention writeups in code comments or other docs.

## What to Change

### 1. Author `docs/engine-environment-isolation.md`

One-page reference covering:

- **Browser-safe subpath exports**: `./` (kernel/index — forward-compatible; runner does not import today), `./runtime`, `./agents`, `./trace`. Document the empirical runner usage as of 2026-05-05 (`./runtime` 66 imports, `./trace` 3, `./agents` 1, `./` 0).
- **Node-only subpath exports**: `./cnl`, `./sim`, plus future explicit Node-loader paths.
- **Naming convention**: any file with top-level `node:*` imports MUST be named `<base>-node-loader.ts`. Exception: files under designated Node-only directories (per the audit finalized in 001, currently `packages/engine/src/cnl/**/*.ts`) are exempt — list the exact ignore paths matching the ESLint rule (003).
- **Split pattern**: when a Node-only API needs to live alongside browser-safe code, follow the PR #235 shape — keep browser-safe core in `<name>.ts`, move file IO + auto-init to `<name>-node-loader.ts`. The loader imports from the core; the core never imports from the loader.
- **Caller responsibility**: tests, CLI scripts, and Node-side bootstrap call into `*-node-loader.ts` files via deep imports. They MUST NOT be re-exported from any browser-safe barrel.
- **Why the rule exists**: brief paragraph on the vite/rollup `__vite-browser-external` failure mode (PR #235 Clusters 3 + 4, commit `6fce497e`) and why per-file classification (rather than alias-to-stub or `sideEffects: false`) is the chosen enforcement.
- **Inventory of `*-node-loader.ts` files** (post-001): list every file matching `packages/engine/src/**/*-node-loader.ts` so the doc is a complete classification.

### 2. Verify cross-references

- Confirm the ESLint rule's error message (added in 003) references this doc by relative path `docs/engine-environment-isolation.md`.
- Confirm spec 151 references this doc consistently (the spec already cites it; this is a cross-check).

## Files to Touch

- `docs/engine-environment-isolation.md` (new)

## Out of Scope

- The ESLint rule itself (003).
- Splitting the engine into a separate `@ludoforge/engine-browser` package (spec line 171 out of scope).
- Dual ESM builds (spec line 172 out of scope).
- Documentation of `node:crypto` → `crypto.subtle.digest` migration paths (spec line 173 out of scope).
- Smoke-test infrastructure (004).

## Acceptance Criteria

### Tests That Must Pass

1. `docs/engine-environment-isolation.md` exists and is approximately one page.
2. The doc's ignore-path list matches the ESLint rule's `ignores` array (003) once both tickets are merged. Cross-checked during 003 implementation.
3. `pnpm turbo lint` passes — no markdown linter regressions.

### Invariants

1. Every `*-node-loader.ts` file in `packages/engine/src/` is mentioned by the doc's classification list, OR is covered by a documented directory ignore.
2. Every engine subpath export in `packages/engine/package.json` is classified in the doc as browser-safe or Node-only.

## Test Plan

### New/Modified Tests

No new automated tests. Doc-only ticket; correctness is reviewed by reading.

### Commands

1. `pnpm turbo lint` — confirms no lint regression.
2. Manual review by the implementer: re-read the doc and confirm it accurately describes the post-001 file layout and matches the planned 003 ignore list.
