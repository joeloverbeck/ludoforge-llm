# DIAGFITL-005: Investigate and fix empty Rally target selection

**Status**: PENDING
**Priority**: LOW
**Effort**: Medium
**Engine Changes**: Possibly — depends on investigation outcome
**Deps**: `archive/tickets/DIAGFITL-002.md`, `archive/tickets/DIAGFITL-003.md`

## Problem

In seed 1000 (Decision 4), the VC agent selected a Rally move with `$targetSpaces: []` — an empty target list that results in a no-op. Despite 10 legal move types being available (22 candidates after completion), the agent chose a zero-target Rally. The `preferPopulousTargets` completion-scope consideration should score more targets higher, but it didn't prevent zero-target selection.

From the trace:
- `initialCandidateCount: 22`, `legalMoveCount: 10`
- `finalScore: 3` (same as other Rally moves with targets)
- Pruning: `dropPassWhenOtherMovesExist` → 21 remaining (1 pass dropped)
- Tie-break: 3 candidates tied, `preferCheapTargetSpaces` didn't differentiate, `stableMoveKey` selected index 12

This suggests either:
1. The `preferPopulousTargets` consideration does not penalize zero-target selections
2. Zero-target Rally has the same action-level score (from `preferRallyWeighted`) as non-zero Rally, and the completion-scope differentiation is insufficient
3. The legal-move enumeration should not generate zero-target Rally as a valid move

## Assumption Reassessment (2026-04-02)

1. Rally allows `chooseN` for `$targetSpaces` with `min: 0` — this permits empty selection. Need to verify against `30-rules-actions.md`.
2. `preferPopulousTargets` is a completion-scope consideration — it evaluates per-completion-decision, not per-completed-move. Need to verify its weight relative to action-level considerations.
3. Zero-target Rally may be intentionally legal (choosing to Rally but selecting no spaces) or unintentionally generated by the completion system.

## Architecture Check

1. This may be a game-spec issue (Rally `chooseN` min should be 1, not 0) or an agent consideration weighting issue
2. Investigation needed before determining the fix category
3. No engine architectural change should be needed — the completion and consideration systems are working as designed

## What to Change

### 1. Investigate Rally chooseN min parameter

Read the Rally action pipeline in `30-rules-actions.md` to determine if `$targetSpaces` `chooseN` has `min: 0`. If so, determine whether this is intentional (some game states may have no valid targets) or a data error.

### 2. Investigate preferPopulousTargets scoring

Read the `preferPopulousTargets` consideration in `92-agents.md` to understand:
- What scope it operates in (completion)
- What it scores (number of tokens in target space)
- Whether it handles the empty-selection case

### 3. Apply fix based on investigation

- **If min:0 is wrong**: Change to `min: 1` in the Rally action definition
- **If min:0 is intentional**: Add a pruning rule or consideration that penalizes zero-target selections
- **If preferPopulousTargets is insufficient**: Adjust its formula or weight

## Files to Touch

- `data/games/fire-in-the-lake/30-rules-actions.md` (possibly modify)
- `data/games/fire-in-the-lake/92-agents.md` (possibly modify)
- Golden fixture files (modify — regenerate if game spec changes)

## Out of Scope

- Changing the completion system's move enumeration logic
- Modifying Rally behavior for non-VC factions
- Adding new action types or alternatives to Rally

## Acceptance Criteria

### Tests That Must Pass

1. After fix: VC agent should not select zero-target Rally when non-zero-target Rally is available
2. Compile FITL game spec — no compiler errors
3. Existing suite: `pnpm -F @ludoforge/engine test`

### Invariants

1. Rally remains legal with valid targets in all game states where VC can rally
2. The fix does not change legality for other factions

## Test Plan

### New/Modified Tests

1. Investigation-dependent — will be determined after root cause is identified

### Commands

1. `pnpm -F @ludoforge/engine build`
2. `pnpm -F @ludoforge/engine test`
3. `pnpm turbo typecheck`
